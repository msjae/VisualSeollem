name: CI

on:
  push:
    branches: [ "CI" ]
  pull_request:
    branches: [ "CI" ]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Build
      run: |
        ./build.sh

    - name: KITTI
      run: Examples/Monocular/mono_kitti Vocabulary/ORBvoc.txt Examples/Monocular/KITTI00-02.yaml dataset/sequences/04
      
